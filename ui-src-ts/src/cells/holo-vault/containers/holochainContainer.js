import { connect } from 'react-redux'
import Holochain from '../components/holochain/holochain'


const mapStateToProps = state => {
  return {
  }
}

const mapDispatchToProps = (dispatch) => {
  return {
  }
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(Holochain)
