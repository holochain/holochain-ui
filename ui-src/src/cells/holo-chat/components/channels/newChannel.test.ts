// import React from 'react'
// import Chasnnels from './channels'
// import { shallow } from 'enzyme'

export const newChannelTests = describe('Creating a new channel', () => {

  it('The New Channel view shows an empty list until you start to filter the list', () => {
    expect(3).toEqual(3)
  })

  it('The filter is cleared', () => {
    expect(3).toEqual(3)
  })
})
