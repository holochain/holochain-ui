// import React from 'react'
// import Chasnnels from './channels'
// import { shallow } from 'enzyme'

export const selectAgentTests = describe('Select who to start chatting with', () => {

  it('Click someone\'s name and a new Chat will start with that person.', () => {
    expect(3).toEqual(3)
  })

})
