
export const presenceArcsTests = describe('Feeling present', () => {

  it('A green arc shows for each persons availablity to be present with the channel', () => {
    expect(3).toEqual(3)
  })

  it('The red sections indicate not available to be present with the channel', () => {
    expect(3).toEqual(3)
  })

  it('The clock hour indicator is your local time', () => {
    expect(3).toEqual(3)
  })
})
